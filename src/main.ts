import {provide} from '@angular/core';
import {bootstrap} from '@angular/platform-browser';

import {ElseqAppComponent} from './elseq.component';


document.addEventListener('DOMContentLoaded', () => bootstrap(ElseqAppComponent));
